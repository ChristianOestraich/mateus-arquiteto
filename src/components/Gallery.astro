---
// Gallery.astro - Galeria de Serviços de Arquitetura - ATUALIZADO COM PALETA TERRA COTA
const services = [
  {
    title: "Projetos Residenciais",
    description: "Mais de 50 projetos",
    image: "../../public/banners/image-gallery/gallery-00.jpeg",
    buttonText: "Saiba Mais",
    link: "/services/residencial"
  },
  {
    title: "Design de Interiores",
    description: "Mais de 30 designs",
    image: "../../public/banners/image-gallery/gallery-01.jpeg",
    buttonText: "Ver Detalhes",
    link: "/services/residencial"
  },
  {
    title: "Projetos Comerciais",
    description: "Mais de 20 projetos",
    image: "../../public/banners/image-gallery/gallery-02.jpeg",
    buttonText: "Descubra",
    link: "/services/residencial"
  },
  {
    title: "Consultoria Estratégica",
    description: "Soluções inovadoras",
    image: "../../public/banners/image-gallery/gallery-03.jpeg",
    buttonText: "Explore Agora",
    link: "/services/residencial"
  },
  {
    title: "Estilo Moderno",
    description: "Tendências atuais",
    image: "../../public/banners/image-gallery/gallery-04.jpeg",
    buttonText: "Ver Mais",
    link: "/services/residencial"
  },
  {
    title: "Preservação Patrimonial",
    description: "Expertise completa",
    image: "../../public/banners/image-gallery/gallery-05.jpeg",
    buttonText: "Saiba Mais",
    link: "/services/residencial"
  }
];
---

<section class="gallery">
  <div class="gallery-container">
    <div class="gallery-header">
      <h2 class="gallery-title">Serviços de Arquitetura</h2>
      <p class="gallery-description">
        Descubra as soluções criativas em arquitetura que transformam espaços em arte. Consulte
        nossos serviços para projetos residenciais e comerciais, parcerias estratégicas e designs
        personalizados que destacam inovação e funcionalidade.
      </p>
    </div>

    <div class="services-grid">
      {services.map((service) => (
        <div class="service-card">
          <div class="card-image">
            <img src={service.image} alt={service.title} loading="lazy" />
            <div class="image-overlay"></div>
          </div>
          <div class="card-content">
            <h3 class="card-title">{service.title}</h3>
            <p class="card-description">{service.description}</p>
          </div>
          <a href={service.link} class="card-button">
            {service.buttonText}
          </a>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .gallery {
    background: #FAF8F5; /* MUDANÇA: #706d6d → #FAF8F5 (BG Cream) */
    padding: 80px 20px;
    min-height: 100vh;
  }

  .gallery-container {
    max-width: 1400px;
    margin: 0 auto;
  }

  .gallery-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .gallery-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 600;
    color: #2C3E50; /* MUDANÇA: #1a1a1a → #2C3E50 (Secondary) */
    margin-bottom: 20px;
  }

  .gallery-description {
    font-size: clamp(0.95rem, 1.5vw, 1.1rem);
    color: #7F8C8D; /* MUDANÇA: #000000 → #7F8C8D (Neutral) */
    max-width: 900px;
    margin: 0 auto;
    line-height: 1.8;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 30px;
    margin-top: 40px;
  }

  .service-card {
    position: relative;
    background: #FFFFFF; /* MUDANÇA: #f8f8f8 → #FFFFFF */
    border-radius: 20px;
    overflow: hidden;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08); /* NOVO: Sombra inicial */
  }

  .service-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(227, 115, 94, 0.15); /* MUDANÇA: Sombra terra cota */
  }

  .card-image {
    position: relative;
    width: 100%;
    height: 280px;
    overflow: hidden;
    background: #ECF0F1; /* MUDANÇA: #e0e0e0 → #ECF0F1 */
  }

  .card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.4s ease; /* NOVO: Transição suave */
  }

  .service-card:hover .card-image img {
    transform: scale(1.05);
  }

  .image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, transparent 0%, rgba(227, 115, 94, 0.2) 100%); /* MUDANÇA: Overlay terra cota */
  }

  .card-content {
    padding: 24px;
  }

  .card-title {
    font-size: 1.4rem;
    font-weight: 600;
    color: #2C3E50; /* MUDANÇA: #1a1a1a → #2C3E50 (Secondary) */
    margin-bottom: 8px;
  }

  .card-description {
    font-size: 0.95rem;
    color: #7F8C8D; /* MUDANÇA: #666 → #7F8C8D (Neutral) */
    margin-bottom: 20px;
  }

  .card-button {
    display: block;
    width: auto;
    max-width: 200px;
    margin: 0 auto 24px;
    padding: 12px 32px;
    background: #E3735E; /* MUDANÇA: #1a1a1a → #E3735E (TERRA COTA) */
    color: #ffffff;
    border: none;
    border-radius: 50px;
    font-size: 0.95rem;
    font-weight: 600; /* MUDANÇA: 500 → 600 */
    cursor: pointer;
    transition: all 0.3s ease;
    text-align: center;
    text-decoration: none;
  }

  .card-button:hover {
    background: #C85A45; /* MUDANÇA: #333 → #C85A45 (Terra Cota Dark) */
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(227, 115, 94, 0.4); /* MUDANÇA: Sombra terra cota */
  }

  @media (max-width: 768px) {
    .gallery {
      padding: 60px 20px;
    }

    .services-grid {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .gallery-header {
      margin-bottom: 40px;
    }
  }

  @media (min-width: 769px) and (max-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1025px) {
    .services-grid {
      grid-template-columns: repeat(4, 1fr);
    }

    .service-card:nth-child(5),
    .service-card:nth-child(6) {
      grid-column: span 2;
    }
  }
</style>

<script>
  // Navegação direta para páginas de serviços
  document.querySelectorAll('.card-button').forEach(button => {
    button.addEventListener('click', (e) => {
      const href = button.getAttribute('href');
      
      // Se for link interno, scroll suave
      if (href?.startsWith('#')) {
        e.preventDefault();
        const targetId = href.substring(1);
        const targetElement = document.getElementById(targetId);
        
        if (targetElement) {
          targetElement.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      }
      // Se for link externo (páginas), deixa navegação padrão acontecer
    });
  });
</script>