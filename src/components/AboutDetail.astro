---
// AboutDetail.astro - Página Sobre Nós Moderna
const stats = [
  { number: "150+", label: "Projetos Concluídos", icon: "check" },
  { number: "15+", label: "Anos de Experiência", icon: "calendar" },
  { number: "98%", label: "Clientes Satisfeitos", icon: "heart" },
  { number: "25+", label: "Prêmios Recebidos", icon: "award" }
];

const values = [
  {
    icon: "star",
    title: "Inovação",
    description: "Buscamos constantemente novas soluções e tecnologias para criar projetos únicos e vanguardistas."
  },
  {
    icon: "shield",
    title: "Excelência",
    description: "Comprometimento com a qualidade em cada detalhe, desde o conceito até a execução final."
  },
  {
    icon: "leaf",
    title: "Sustentabilidade",
    description: "Projetos responsáveis que respeitam o meio ambiente e promovem eficiência energética."
  },
  {
    icon: "users",
    title: "Colaboração",
    description: "Trabalhamos em parceria com nossos clientes para transformar sonhos em realidade."
  }
];

const team = [
  {
    name: "Ricardo Santos",
    role: "Arquiteto Fundador",
    image: "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=600&q=80",
    bio: "Mais de 15 anos criando espaços que inspiram"
  },
  {
    name: "Ana Paula Costa",
    role: "Designer de Interiores",
    image: "https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=600&q=80",
    bio: "Especialista em transformar ambientes em experiências"
  },
  {
    name: "Carlos Mendes",
    role: "Gerente de Projetos",
    image: "https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=600&q=80",
    bio: "Garantindo excelência em cada etapa"
  }
];
---

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width" />
  <title>Sobre Nós | Arquitetura</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #ffffff;
      color: #1a1a1a;
      overflow-x: hidden;
    }

    /* Menu Hamburguer */
    .burger {
      position: fixed;
      z-index: 1000;
      top: 0;
      left: 0;
      display: grid;
      place-items: center;
      width: 100px;
      height: 100px;
      border: 0;
      padding: 0;
      background: transparent;
      cursor: pointer;
    }

    .burger-icon {
      position: relative;
      width: 16px;
      height: 2px;
      background: #ffffff;
      transition: 0.3s;
    }

    .burger-icon::before,
    .burger-icon::after {
      content: "";
      position: absolute;
      left: 0;
      width: 16px;
      height: 2px;
      background: #ffffff;
      transition: 0.3s;
    }

    .burger-icon::before {
      top: -8px;
    }

    .burger-icon::after {
      top: 8px;
    }

    .menu {
      position: fixed;
      z-index: 999;
      top: 0;
      left: 0;
      display: grid;
      place-items: center;
      width: 50%;
      max-width: 400px;
      height: 100%;
      background: rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(10px);
      transform: translateX(-100%);
      transition: transform 0.425s cubic-bezier(0.175, 0.885, 0.32, 1);
      opacity: 0;
      visibility: hidden;
    }

    .menu.open {
      transform: translateX(0);
      opacity: 1;
      visibility: visible;
    }

    .menu nav {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
      opacity: 0;
      transition: opacity 0.3s ease 0.2s;
    }

    .menu.open nav {
      opacity: 1;
    }

    .menu a {
      position: relative;
      color: #f9f9f9;
      font-size: 30px;
      padding: 18px 0;
      width: 100%;
      text-decoration: none;
      text-align: center;
      transition: 0.4s;
    }

    .menu a::before,
    .menu a::after {
      content: "";
      position: absolute;
      bottom: 10px;
      width: 100%;
      height: 2px;
      transition: 0.4s;
    }

    .menu a::before {
      opacity: 0;
      background: rgba(255, 255, 255, 0.2);
    }

    .menu a::after {
      transform: scaleX(0);
      transform-origin: 0% 50%;
      background: #f7f7f7;
    }

    .menu a:hover::before {
      opacity: 1;
    }

    .menu a:hover::after {
      transform: scaleX(1);
    }

    body.menu-open .burger-icon {
      background: #ffffff;
      transform: rotate(-45deg);
    }

    body.menu-open .burger-icon::before {
      background: #ffffff;
      transform: rotate(90deg) translate(10px, -1px);
    }

    body.menu-open .burger-icon::after {
      background: #ffffff;
      transform: rotate(90deg) translate(-9px, 1px);
    }

    /* Hero Section */
    .hero-section {
      position: relative;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
      overflow: hidden;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      text-align: center;
      color: #ffffff;
      max-width: 900px;
      padding: 0 20px;
    }

    .hero-subtitle {
      font-size: clamp(0.9rem, 2vw, 1.1rem);
      color: #d4a574;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      margin-bottom: 24px;
      opacity: 0;
      animation: fadeInUp 0.8s ease forwards 0.2s;
    }

    .hero-title {
      font-size: clamp(3rem, 8vw, 6rem);
      font-weight: 900;
      line-height: 1.1;
      margin-bottom: 32px;
      opacity: 0;
      animation: fadeInUp 0.8s ease forwards 0.4s;
    }

    .hero-description {
      font-size: clamp(1.1rem, 2vw, 1.4rem);
      line-height: 1.8;
      color: #cccccc;
      opacity: 0;
      animation: fadeInUp 0.8s ease forwards 0.6s;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .hero-background {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      opacity: 0.1;
      background-image: url('https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?w=1920&q=80');
      background-size: cover;
      background-position: center;
    }

    /* Stats Section */
    .stats-section {
      padding: 100px 20px;
      background: #706d6d;
    }

    .stats-container {
      max-width: 1400px;
      margin: 0 auto;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 40px;
    }

    .stat-card {
      text-align: center;
      padding: 48px 24px;
      background: #ffffff;
      border-radius: 20px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .stat-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
    }

    .stat-icon {
      width: 64px;
      height: 64px;
      margin: 0 auto 24px;
      background: linear-gradient(135deg, #d4a574 0%, #c49564 100%);
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #ffffff;
    }

    .stat-number {
      font-size: 3.5rem;
      font-weight: 900;
      color: #1a1a1a;
      margin-bottom: 12px;
      line-height: 1;
    }

    .stat-label {
      font-size: 1.1rem;
      color: #666;
      font-weight: 500;
    }

    /* Story Section */
    .story-section {
      padding: 120px 20px;
      background: #cbc7c7;
    }

    .story-container {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 80px;
      align-items: center;
    }

    .story-content h2 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      font-weight: 700;
      margin-bottom: 32px;
      color: #1a1a1a;
    }

    .story-content p {
      font-size: 1.2rem;
      line-height: 1.8;
      color: #666;
      margin-bottom: 24px;
    }

    .story-image {
      position: relative;
      height: 600px;
      border-radius: 20px;
      overflow: hidden;
    }

    .story-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    /* Values Section */
    .values-section {
      padding: 100px 20px;
      background: #706d6d;
      color: #ffffff;
    }

    .values-container {
      max-width: 1400px;
      margin: 0 auto;
    }

    .values-header {
      text-align: center;
      margin-bottom: 80px;
    }

    .values-header h2 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      font-weight: 700;
      margin-bottom: 24px;
    }

    .values-header p {
      font-size: 1.2rem;
      color: #cccccc;
      max-width: 700px;
      margin: 0 auto;
    }

    .values-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 32px;
    }

    .value-card {
      padding: 40px 32px;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      transition: all 0.3s ease;
    }

    .value-card:hover {
      background: rgba(255, 255, 255, 0.1);
      transform: translateY(-8px);
    }

    .value-icon {
      width: 56px;
      height: 56px;
      background: #d4a574;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 24px;
      color: #1a1a1a;
    }

    .value-card h3 {
      font-size: 1.5rem;
      margin-bottom: 16px;
    }

    .value-card p {
      font-size: 1rem;
      line-height: 1.6;
      color: #cccccc;
    }

    /* Team Section */
    .team-section {
      padding: 120px 20px;
      background: #cbc7c7;
    }

    .team-container {
      max-width: 1400px;
      margin: 0 auto;
    }

    .team-header {
      text-align: center;
      margin-bottom: 80px;
    }

    .team-header h2 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      font-weight: 700;
      margin-bottom: 24px;
      color: #1a1a1a;
    }

    .team-header p {
      font-size: 1.2rem;
      color: #666;
      max-width: 700px;
      margin: 0 auto;
    }

    .team-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 48px;
    }

    .team-card {
      position: relative;
      overflow: hidden;
      border-radius: 20px;
      transition: transform 0.4s ease;
    }

    .team-card:hover {
      transform: translateY(-12px);
    }

    .team-image {
      position: relative;
      width: 100%;
      aspect-ratio: 1;
      overflow: hidden;
    }

    .team-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.6s ease;
    }

    .team-card:hover .team-image img {
      transform: scale(1.1);
    }

    .team-info {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      padding: 32px;
      background: linear-gradient(to top, rgba(0, 0, 0, 0.9) 0%, transparent 100%);
      color: #ffffff;
      transform: translateY(20px);
      transition: transform 0.4s ease;
    }

    .team-card:hover .team-info {
      transform: translateY(0);
    }

    .team-name {
      font-size: 1.6rem;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .team-role {
      font-size: 1rem;
      color: #d4a574;
      margin-bottom: 12px;
    }

    .team-bio {
      font-size: 0.95rem;
      opacity: 0.9;
      line-height: 1.5;
    }

    /* CTA Section */
    .cta-section {
      padding: 100px 20px;
      background: linear-gradient(135deg, #2d3b3e 0%, #3a4a4e 100%);
      text-align: center;
      color: #ffffff;
    }

    .cta-container {
      max-width: 900px;
      margin: 0 auto;
    }

    .cta-title {
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 700;
      margin-bottom: 24px;
    }

    .cta-description {
      font-size: 1.3rem;
      color: #cccccc;
      margin-bottom: 48px;
      line-height: 1.8;
    }

    .cta-button {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      padding: 18px 48px;
      background: #d4a574;
      color: #1a1a1a;
      border-radius: 50px;
      font-size: 1.2rem;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .cta-button:hover {
      background: #c49564;
      transform: translateY(-3px);
      box-shadow: 0 12px 30px rgba(212, 165, 116, 0.4);
    }

    @media (max-width: 1024px) {
      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .story-container {
        grid-template-columns: 1fr;
      }

      .values-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .team-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 768px) {
      .stats-grid,
      .values-grid,
      .team-grid {
        grid-template-columns: 1fr;
      }

      .story-image {
        height: 400px;
      }
    }
  </style>
</head>
<body>
  <!-- Menu Hamburguer -->
  <button class="burger" id="burger" aria-label="Toggle menu">
    <span class="burger-icon"></span>
  </button>

  <div class="menu" id="menu">
    <nav>
      <a href="/">Home</a>
      <a href="/services/residencial">Serviços</a>
      <a href="/#portfolio">Portfolio</a>
      <a href="/feedback/feedback">Feedback</a>
      <a href="/contact/contato">Contato</a>
    </nav>
  </div>

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="hero-background"></div>
    <div class="hero-content">
      <p class="hero-subtitle">Sobre Nós</p>
      <h1 class="hero-title">Criando Espaços que Inspiram</h1>
      <p class="hero-description">
        Transformamos sonhos em realidade através da arquitetura inovadora e design excepcional
      </p>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="stats-section">
    <div class="stats-container">
      <div class="stats-grid">
        {stats.map((stat) => (
          <div class="stat-card">
            <div class="stat-icon">
              {stat.icon === 'check' && (
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                  <polyline points="20 6 9 17 4 12"/>
                </svg>
              )}
              {stat.icon === 'calendar' && (
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                  <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                  <line x1="16" y1="2" x2="16" y2="6"/>
                  <line x1="8" y1="2" x2="8" y2="6"/>
                  <line x1="3" y1="10" x2="21" y2="10"/>
                </svg>
              )}
              {stat.icon === 'heart' && (
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                  <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
                </svg>
              )}
              {stat.icon === 'award' && (
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                  <circle cx="12" cy="8" r="7"/>
                  <polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88"/>
                </svg>
              )}
            </div>
            <div class="stat-number">{stat.number}</div>
            <div class="stat-label">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Story Section -->
  <section class="story-section">
    <div class="story-container">
      <div class="story-content">
        <h2>Nossa História</h2>
        <p>
          Fundada em 2010, nossa empresa nasceu da paixão por criar espaços que transformam vidas. 
          Começamos com um pequeno escritório e um grande sonho: revolucionar a arquitetura moderna.
        </p>
        <p>
          Hoje, somos reconhecidos nacionalmente por nossos projetos inovadores que combinam 
          estética, funcionalidade e sustentabilidade. Cada projeto é uma nova oportunidade de 
          superar expectativas e criar algo verdadeiramente excepcional.
        </p>
        <p>
          Nossa abordagem centrada no cliente e atenção aos detalhes nos permitiu construir 
          relacionamentos duradouros e uma reputação de excelência no mercado.
        </p>
      </div>
      <div class="story-image">
        <img src="https://images.unsplash.com/photo-1600585154340-be6161a56a0c?w=800&q=80" alt="Nossa História" />
      </div>
    </div>
  </section>

  <!-- Values Section -->
  <section class="values-section">
    <div class="values-container">
      <div class="values-header">
        <h2>Nossos Valores</h2>
        <p>Os princípios que guiam cada decisão e projeto que realizamos</p>
      </div>
      <div class="values-grid">
        {values.map((value) => (
          <div class="value-card">
            <div class="value-icon">
              {value.icon === 'star' && (
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
                </svg>
              )}
              {value.icon === 'shield' && (
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
                </svg>
              )}
              {value.icon === 'leaf' && (
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.48 19 2c1 2 2 4.18 2 8 0 5.5-4.78 10-10 10Z"/>
                  <path d="M2 21c0-3 1.85-5.36 5.08-6C9.5 14.52 12 13 13 12"/>
                </svg>
              )}
              {value.icon === 'users' && (
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                  <circle cx="9" cy="7" r="4"/>
                  <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
                  <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                </svg>
              )}
            </div>
            <h3>{value.title}</h3>
            <p>{value.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Team Section -->
  <section class="team-section">
    <div class="team-container">
      <div class="team-header">
        <h2>Nossa Equipe</h2>
        <p>Profissionais apaixonados dedicados a transformar suas visões em realidade</p>
      </div>
      <div class="team-grid">
        {team.map((member) => (
          <div class="team-card">
            <div class="team-image">
              <img src={member.image} alt={member.name} />
            </div>
            <div class="team-info">
              <h3 class="team-name">{member.name}</h3>
              <p class="team-role">{member.role}</p>
              <p class="team-bio">{member.bio}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="cta-container">
      <h2 class="cta-title">Vamos Criar Algo Incrível Juntos?</h2>
      <p class="cta-description">
        Estamos prontos para transformar sua visão em realidade. Entre em contato e 
        vamos começar seu próximo projeto extraordinário.
      </p>
      <a href="/contact" class="cta-button">
        Iniciar Projeto
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </section>

  <script>
    // Menu hamburguer
    const burger = document.getElementById('burger');
    const menu = document.getElementById('menu');

    burger?.addEventListener('click', () => {
      document.body.classList.toggle('menu-open');
      menu?.classList.toggle('open');
    });

    // Fecha menu ao clicar nos links
    document.querySelectorAll('.menu a').forEach(link => {
      link.addEventListener('click', () => {
        document.body.classList.remove('menu-open');
        menu?.classList.remove('open');
      });
    });

    // Fecha menu com Escape
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && document.body.classList.contains('menu-open')) {
        document.body.classList.remove('menu-open');
        menu?.classList.remove('open');
      }
    });
  </script>
</body>
</html>